services:
  agent-cool:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: livekit-agent-cool
    env_file:
      - .env.local
    network_mode: "host" 
    restart: unless-stopped